const num1 = 100;
const num2 = 50;

// Simple math with numbers
val = num1 + num2; // Add 
val = num1 * num2; // Multiply
val = num1 - num2; // Subtract
val = num1 / num2; // Dived
val = num1 % num2; // Modulus

// Math Object
val = Math.PI;
val = Math.E;
val = Math.round(2.4);
val = Math.ceil(2.4);
val = Math.floor(2.8);
val = Math.sqrt(64);
val = Math.abs(-3);
val = Math.pow(8, 2);
val = Math.min(2,1,3,4,77,33,22);
val = Math.max(2,1,3,4,77,33,22);
val = Math.random(); // Will return random decimal number

val = Math.floor(Math.random() * 20 + 1); // Must wrap .random() with .floor() to return a whole number






console.log(val);